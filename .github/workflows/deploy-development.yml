name: Deploy Development

on:
  workflow_dispatch:

concurrency:
  group: deploy-development-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    name: CI
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: development

      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm

      - run: npm ci --legacy-peer-deps

      - run: npm run format:check
      - run: npm run lint
      - run: npm test
      - run: npm run build

  cd:
    name: CD
    needs: ci
    runs-on: ubuntu-latest
    environment: development
    steps:
      - uses: actions/checkout@v5
        with:
          ref: development

      - name: Upload helpdesk to VPS
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          password: ${{ secrets.VPS_PASSWORD }}
          port: 22
          source: '.'
          target: /opt/coffee-cherry/development/helpdesk
          overwrite: true

      - name: Deploy helpdesk to VPS
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          password: ${{ secrets.VPS_PASSWORD }}
          port: 22
          command_timeout: 30m
          script: |
            set -e
            cd /opt/coffee-cherry/development
            docker compose up -d --build helpdesk
            docker compose ps helpdesk
